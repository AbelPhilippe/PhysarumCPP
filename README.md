<img width="100%" src="https://capsule-render.vercel.app/api?type=waving&color=CCFF00&height=100&section=header">

<div align= center>
<img width="800" height="526" alt="image" src= "https://github.com/user-attachments/assets/66787d55-0a28-43af-984d-eeff13fb65e0" />
</div>

<img width="100%" src="https://capsule-render.vercel.app/api?type=waving&color=CCFF00&height=100&section=footer">
<h1 align=center>
  What is Physarum?
</h1>

<div align="left">
- Physarum, or more precisely Physarum polycephalum, is an acellular slime mold, or myxogastrid. Myxogastrids are closely related to the cellular slime molds, or dictyostelids, including the well known Dictyostelium discoideum. Physarum is more distantly related to the protostelids (together these groups are discussed as the eumycetozoans, or ‘true’ slime molds) and very distantly related to other species also called slime molds, for example acrasids.
</div>

## Requirements
- C++ compiler (g++)
- OpenGL
- GLFW

## Clone Repository
```bash
git clone -v https://github.com/AbelPhilippe/PhysarumCPP
cd PhysaryumCPP
```
## Install.sh
```bash
chmod +X install.sh
./install.sh
```
## If you use VScode debugger: Tasks.json (you need use g++ compiler)
```json
{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "C/C++: gcc build active file",
            "command": "/usr/bin/g++",
            "args": [
                "-fdiagnostics-color=always",
                "-g",
                "${workspaceFolder}/*.cpp",
                "${workspaceFolder}/glad/*.c",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}",
                "-lGL",
                "-lglfw"
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Task generated by Debugger."
        }
    ],
    "version": "2.0.0"
}
```
## Build 1: CMAKE
```bash
mkdir build
cd build
cmake ..
cmake --build .
./sim
```
## Build 2: 
### Linux:
```bash
g++ main.cpp glad/glad.c -o sim -lglfw -lGL -ldl
```
### Windows:
```bash
g++ main.cpp glad.c -o sim.exe -lglfw3 -lopengl32 -lgdi32
```

<h3 align="center">
    To Do - Ideas
</h3>

- [x] Colours
- [ ] Game concept
- [ ] Output modeling